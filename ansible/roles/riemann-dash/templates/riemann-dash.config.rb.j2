# Serve HTTP traffic on this port
set  :port, 4567
# Answer queries sent to this IP address
set  :bind, "0.0.0.0"
# Specify custom workspace config
config[:ws_config] = '{{ riemann_base_dir }}/config/riemann-dash.ui.config.json'
